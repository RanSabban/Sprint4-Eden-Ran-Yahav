export function GroupStatistics({tasks,index}) {

    function getMap() {

    }
    
    return <div>
        {/* {
                cells.map((cell, idx) => (

                    <DynamicCmp key={idx}
                        groupId={groupId}
                        groupColor={groupColor}
                        cmpType={cell.type}
                        onChange={onChange}
                        clmType={getClmType(cell._id)}
                        setSelectedCell={setSelectedCell}
                        cell={cell}
                        onUpdateCell={onUpdateCell}
                        taskId={task._id}
                        onClick={openDynModal}
                        onClickLabel={onClickLabel}
                        // selectedCell={selectedCell}
                    />
                ))
            }  */}

    </div>
}